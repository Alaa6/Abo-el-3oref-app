<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- *********this two permission to user image cropper libarary************ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SilverActivity"></activity>
        <activity android:name=".GoldenActivity"></activity>
        <activity android:name=".BronzeActivity" />
        <activity android:name=".allComplainActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AboEl3orif" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".SavedCommentActivity" />
        <activity android:name=".Manage_Catigories" />

        <activity android:name=".Edit_Specific_Category_Place" />
        <activity android:name=".Manage_categories_childern"></activity>
        <activity android:name=".Edit_Delete_All_Categories" />
        <activity android:name=".Manage_Areas" />
        <activity android:name=".Edit_specific_area" />
        <activity android:name=".Edit_Delete_All_Areas" />
        <activity android:name=".Profile3" />
        <activity android:name=".Profile2" />
        <activity android:name=".Profile1" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".AllUsersActivity" />
        <activity android:name=".EditQuestionCommentActivity" />
        <activity android:name=".QuestionCommentsActivity" />
        <activity android:name=".EditQuestionActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".custompopupActivity" />
        <activity android:name=".PostsActivity" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".StartActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps_get_user_location"
            android:label="@string/title_activity_maps_get_user_location" />
        <activity android:name=".Add_Category_Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".GoogleMapsActivity" />
        <activity android:name=".setup_user_Activity" /> <!-- ******************* to user image cropper******************************** -->
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" /> <!-- ******************* ***************************************************** -->
        <activity android:name=".Register_Activity" />
        <activity android:name=".Add_Area_Activirt" />
        <activity android:name=".EditPostActivity" />
        <activity android:name=".Comment" />
        <activity android:name=".FavoriteTemp" />
        <activity android:name=".NewPostActivity" />
        <activity android:name=".MainSecondActivity" />
        <activity android:name=".ClickPostActivity" />
        <activity android:name=".CommentSecondActivity" />
        <activity android:name=".ComplainActivity" />
        <activity android:name=".MessageActivity" />
        <activity
            android:name=".EditComment"
            android:label="@string/title_activity_edit_comment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- face auth -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider342183503136386"
            android:exported="true" />
    </application>

</manifest>